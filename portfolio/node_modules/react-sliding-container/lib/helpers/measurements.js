"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
;
exports.measurements = function (doc, margin) {
    var wrapper = doc.querySelector('#react-sliding-container');
    var leftArrow = doc.querySelector('#rscawl>div');
    var rightArrow = doc.querySelector('#rscawl>div');
    var wrapperWidth = wrapper ? wrapper.clientWidth : null;
    var leftArrowWidth = leftArrow ? leftArrow.clientWidth : null;
    var rightArrowWidth = rightArrow ? rightArrow.clientWidth : null;
    return {
        arrows: {
            left: {
                width: leftArrowWidth,
            },
            right: {
                width: rightArrowWidth,
            }
        },
        slide: {
            left: leftArrowWidth + margin,
            right: rightArrowWidth + margin,
            width: wrapperWidth - leftArrowWidth - rightArrowWidth - (margin * 2),
        },
        wrapper: {
            width: wrapperWidth,
        }
    };
};
//# sourceMappingURL=measurements.js.map